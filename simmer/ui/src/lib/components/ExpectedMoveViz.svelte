<script lang="ts">
	// TODO(charts pass): expected-move vs short-strike placement strip, inline
	// SVG, surfacing BOTH POP numbers (market-implied and forecast — the gap is
	// the edge). Stubbed pending the dedicated dataviz pass (docs/simmer.md,
	// Phase 4 item 32 — load the `dataviz` skill before building).
	import type { ReadinessEnvelope } from '$lib/types';

	let { env }: { env: ReadinessEnvelope } = $props();
</script>

<!-- viz intentionally not rendered yet -->
